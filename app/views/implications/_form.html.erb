<%= form_for(@implication) do |f| %>
  <% if @implication.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@implication.errors.count, "error") %> prohibited this implication from being saved:</h2>

      <ul>
      <% @implication.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tag_1 %><br />
    <%= f.text_field :tag_1 %>
  </div>
  <div class="field">
    <%= f.label :tag_2 %><br />
    <%= f.text_field :tag_2 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
